# VoiceLink Environment Variables

# API Configuration
# Backend API endpoint (running on port 3000 by default)
VITE_API_URL=http://localhost:3000/api
VITE_API_TIMEOUT=30000

# Firebase Configuration
# Get these values from your Firebase project settings:
# 1. Go to https://console.firebase.google.com
# 2. Select your project
# 3. Click Settings (gear icon) > Project settings
# 4. Copy the Web app config values below

VITE_FIREBASE_API_KEY=your_api_key_here
VITE_FIREBASE_AUTH_DOMAIN=your_project.firebaseapp.com
VITE_FIREBASE_PROJECT_ID=your_project_id
VITE_FIREBASE_STORAGE_BUCKET=your_project.appspot.com
VITE_FIREBASE_MESSAGING_SENDER_ID=your_messaging_sender_id
VITE_FIREBASE_APP_ID=your_app_id

# IVR Configuration
VITE_IVR_HOTLINE_NUMBER=

# Feature Flags
VITE_ENABLE_DARK_MODE=true
VITE_ENABLE_VOICE_FEATURES=true
VITE_ENABLE_FIREBASE=true

# Firebase Setup Instructions:
# 1. Create a .env file from this template: cp .env.example .env
# 2. Get Firebase credentials from Firebase Console
# 3. Fill in the Firebase configuration values
# 4. Enable Authentication, Firestore, and Storage in Firebase Console
# 5. Set up Firestore rules for collections: marketPrices, transportSchedules, alerts, users

